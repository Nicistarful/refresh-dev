name: Synk Scan

on:
  push:
    branches:
    - main

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Run Snyk Check
        uses: snyk/actions/iac@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          file: package.json